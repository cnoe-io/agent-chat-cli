name: caipe CLI CI

on:
  pull_request:
  push:
    branches:
      - main
      - "prebuild/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-24.04, macos-14]

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6  # v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bun tsc --noEmit

      - name: Lint (Biome)
        run: bun run lint

      - name: Run tests (unit + e2e)
        run: bun run test

      - name: Build (verify compilation succeeds)
        run: bun build src/index.ts --outdir dist --target node --external keytar --external fsevents

      - name: Cold-start check (<1s)
        run: |
          ms() { python3 -c "import time; print(int(time.time()*1000))"; }
          start=$(ms)
          node dist/index.js --version
          elapsed=$(( $(ms) - start ))
          echo "Cold-start: ${elapsed}ms"
          if [ "$elapsed" -gt 1000 ]; then
            echo "FAIL: cold-start exceeded 1000ms (${elapsed}ms)"
            exit 1
          fi
